<h1>Tracks</h1>
<% if songs %>
  <% songs.each do |s| %>
    <%= s.song.original_filename %><br>
    <div class="track-thumbnail"><%= audio_tag s.song.url, controls: true %></div>
  <% end %>
<% end %>
