<% track = retrack.track %>
<div class="player1">
  <div class="player1-Top">
    <div class="overlay"></div>
      <div class="player1-background">
    	 <%= image_tag track.pic.url %>
      </div>
    <div class="player1-Picture">
      <%= image_tag track.pic.url %>
    </div>
    <div class="player1-Genre">
      <%= time_ago_in_words track.created_at %>
    </div>
  </div>

  <div class="player1-Section2">
    <div class="player1-Section2-Middle">
      <div class="player1-Name">
        <div class="player1-play">
          <div class="playa" song="<%=track.song.url%>" image="<%= track.pic.url%>" artist="<%= track.owner.name%>" name="<%=track.name%>">
            <div class="play-Btn">
              <span class="glyphicon glyphicon-play" aria-hidden="true"></span>
            </div>
          </div>
        </div>
      </div>
      <div class="player1-left-btn">
        <div class="player1-artist">
          <%= link_to track.owner.name, track.owner %> <div class="glyphicon glyphicon-retweet"></div> <%= retrack.reposter.name %>
        </div>
        <div class="player1-songName">
          <%= link_to track.name, track %>
        </div>
      </div>

      <div class="player1-right-btn">
        <div class="player1-btn">
          <span class="glyphicon glyphicon-share" aria-hidden="true"></span>
        </div>

      <% if current_user %>
				<% if track.owner == current_user %>
					<!-- Buttons to show owner -->
          <div class="player1-btn2">
            <%= link_to "", track_delete_path(track), :remote => true, :method => :post, :class => "glyphicon glyphicon-trash", data: { confirm: 'Are you certain you want to delete this?' } %>
          </div>
  			  <% elsif !track.clique_only %>
  					<% if current_user.has_retracked? track %>
  						<!-- Un-repost button -->
                <%= link_to track_repost_path(track), :remote => true, :class => "player1-btn redclass", :method => :post do %>
                  <div class="glyphicon glyphicon-refresh redclass"></div>
                <% end %>
  					<% else %>
  						<!-- Repost button -->
                <%= link_to track_repost_path(track), :remote => true, :class => "player1-btn", :method => :post do %>
                  <div class="glyphicon glyphicon-refresh"></div>
                <% end %>
  					<% end %>
				  <% end %>
					<% if track.favoriters.include? current_user %>
            <!-- Unfavorite button -->
						<%= link_to track_favorite_path(track), :remote => true, :class => "player1-btn redclass", :method => :post do %>
            <div class="glyphicon glyphicon-heart redclass"></div>
            <% end %>
					<% else %>
            <!-- Favorite button -->
						<%= link_to track_favorite_path(track), :remote => true, :class => "player1-btn", :method => :post do %>
              <div class="glyphicon glyphicon-heart"></div>
            <% end %>
					<% end %>
        <% end %>



        <% if track.downloadable %>
          <div class="player1-btn" onclick='myDownload("<%=track.song.url%>","<%=track.name%>")'>
            <span class="glyphicon glyphicon-save" aria-hidden="true"></span>
          </div>
        <% end %>

        <div class="player1-btn" data-toggle="modal" data-target="#track-modal<%= track.id %>">
          <span class="glyphicon glyphicon-comment" aria-hidden="true"></span>
        </div>
      </div>
    </div>
  </div>
  <!-- Comment form outside of modal -->
  <!--<% if current_user %>
    <div class="post-Comment">
      <div class="user-avi1">
        <%= link_to image_tag(current_user.profile_picture_url), current_user %>
      </div>
      <%= form_for TrackComment.new, :remote => true, :html => { :id => "track-comment-form#{track.id}a" } do |form| %>
        <%= form.hidden_field :track_id, :value => track.id %>
        <%= form.text_area :content, :placeholder => "Comment", :class => "comment-field" %>
      <% end %>
    </div>
  <% end %>-->
</div>


<!-- Modal -->
<div class="modal fade" id="track-modal<%= track.id %>" tabindex="-1" role="dialog" aria-labelledby="myModalLabel">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-track-Top">
        <div class="model-track-background">
         <%= image_tag track.pic.url %>
        </div>
        <div class="modal-track-Genre">
          <%= time_ago_in_words track.created_at %>
        </div>
      </div>
      <div class="player1-Section2">
        <div class="player1-Section2-Middle">
          <div class="player1-Name">
            <div class="player1-play">
              <div class="playa" song="<%=track.song.url%>" image="<%= track.pic.url%>" artist="<%= track.owner.name%>" name="<%=track.name%>">
                <div class="play-Btn">
                  <span class="glyphicon glyphicon-play" aria-hidden="true"></span>
                </div>
              </div>
            </div>
          </div>
          <div class="player1-left-btn">
            <div class="player1-artist">
              <%= link_to track.owner.name, track.owner %>
            </div>
            <div class="player1-songName">
              <%= link_to track.name, track %>
            </div>
          </div>
          <div class="player1-right-btn">
          <% if current_user %>
            <% if track.owner == current_user %>
              <!-- Buttons to show owner
              <div class="player1-btn2">
                <%= link_to "", track, :class => "glyphicon glyphicon-trash", :method => :delete, data: { confirm: 'Are you certain you want to delete this?' } %>
              </div>
              <% elsif !track.clique_only %>
                <% if current_user.retracks.include? track %>
                  <!-- Un-repost button
                  <div class="player1-btn">
                    <%= link_to "", track_repost_path(track), :remote => true, :class => "glyphicon glyphicon-refresh", :method => :post %>
                  </div>
                <% else %>
                  <!-- Repost button
                  <div class="player1-btn">
                    <%= link_to "", track_repost_path(track), :remote => true, :class => "glyphicon glyphicon-refresh", :method => :post %>
                  </div>
                <% end %>
              <% end %>
              <div class="player1-btn">
                <% if track.favoriters.include? current_user %>
                  <!-- Unfavorite button
                  <%= link_to "", track_favorite_path(track), :remote => true, :class => "glyphicon glyphicon-heart", :method => :post %>
                <% else %>
                  <!-- Favorite button
                  <%= link_to "", track_favorite_path(track), :remote => true, :class => "glyphicon glyphicon-heart", :method => :post %>
                <% end %>
              </div>
            <% end %>
            <div class="player1-btn">
              <span class="glyphicon glyphicon-share" aria-hidden="true"></span>
            </div>
            <% if track.downloadable %>
              <div class="player1-btn" onclick="window.open('<%= track.song.url%>')">
                <span class="glyphicon glyphicon-save" aria-hidden="true"></span>
              </div>
            <% end %>-->
          </div>
        </div>
      </div>

      <!-- Comments -->
      <%= render :partial => 'tracks/comment_form', :locals => { :track => track } %>
      <div id="track-comments<%= track.id %>" class="modal-post-commentBox">
        <%= render :partial => 'tracks/comments', :locals => { :track => track } %>
      </div>
    </div>
  </div>
</div>
